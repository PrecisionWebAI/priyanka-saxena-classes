import React from "react";

type Props = {};

const StickyNavbar = (props: Props) => {
  return (
    <div className="bg-primary flex min-h-10 sticky top-0 justify-center items-center px-[10%]">
      StickyNavbar
    </div>
  );
};

export default StickyNavbar;
